using System;

namespace bonusPoint
{
    class Program
    {
        static void Main(string[] args)
        {
            int nom = int.Parse(Console.ReadLine());
            double bonus = 0;
            if(nom <)
        }
    }
}
