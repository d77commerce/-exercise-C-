using System;

namespace worldRecord
{
    class Program
    {
        static void Main(string[] args)
        {
            double recInsec = double.Parse(Console.ReadLine());
            double distans = double.Parse(Console.ReadLine());
            double seconds = double.Parse(Console.ReadLine());

            double distansInSecondes = distans * seconds;
            double delay = Math.Floor(distans/) 
             


        }
    }
}
